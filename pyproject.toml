[tool.uv]
workspace.members = ["packages/*"]

[tool.mypy]
packages = ["canny_chenn"]
exclude = ["deprecated"]
ignore_missing_imports = true

[dependency-groups]
dev = [
    "pytest-mypy>=1.0.1",
    "pytest-pretty>=1.3.0",
]

